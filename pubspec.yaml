name: klconnect
description: A news and campus app built with Flutter.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  http: ^1.1.0
  image_picker: ^1.0.4   # ✅ FIXED (moved here)
  firebase_core: ^2.24.0
  cloud_firestore: ^4.13.0
  google_generative_ai: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
